{% extends 'base.html.twig' %}

{% block body %}
    {% include '@Flo/Default/navadmin.html.twig' %}

    <div id="admin">
        <div class="container">
                        <h1>Actualite - Création</h1>

                        {{ form_start(form) }}
                        {{ form_widget(form) }}

                        <div class="boutons text-center">
                            <button class="btn btn-default" type="submit" title="Valider">Valider</button>
                            <a class="btn btn-default" href="{{ path('actualites_index') }}">Annuler</a>
                        </div>

                        {{ form_end(form) }}


        </div>

    </div>

    {% include '@Flo/Default/footer_admin.html.twig' %}

{% endblock %}
