{% extends 'base.html.twig' %}

{% block body %}
    <div class="home">
        <div class="row">
            <div class="col-md-7 col-xs-12">
                <h1 class="name">FLORENCE MENET-PELISSON</h1>
            </div>

            <div class="col-md-4 col-md-offset-8 col-xs-12 ">
                <div class="content">
                    <ul class="bmenu">
                        <li><a href="{{ path('flo_oeuvres') }}">Oeuvres</a></li>
                        <li><a href="{{ path('flo_fictions') }}">Fictions</a></li>
                        <li><a href="{{ path('flo_cours') }}">Cours</a></li>
                        <li><a href="{{ path('flo_actu') }}">Actualités</a></li>
                        <li><a href="{{ path('flo_about') }}">A propos</a></li>
                        <li><a href="{{ path('flo_contact') }}">Contact</a></li>
                        {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                            <li><a class="item" href="{{ path('admin_homepage') }}">Admin</a> </li>
                        {% else %}

                        {% endif %}
                    </ul>
                </div>
            </div>

        </div>
        {% include '@Flo/Default/footer.html.twig' %}

    </div>

{% endblock %}
